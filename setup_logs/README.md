# 📦 Setup Logs

This directory contains **auto-generated install logs** for each team member.

Each file is created by running `./setup.sh <name>` and records:
- What packages were installed
- Machine/OS/Python info
- Venv size and package count
- Full `pip freeze` output

## Files

| File | Person | Updated By |
|------|--------|-----------|
| `shubh_installed.md` | Shubh | Only Shubh's `setup.sh` run |
| `dev_installed.md` | Dev | Only Dev's `setup.sh` run |
| `siddh_installed.md` | Siddh | Only Siddh's `setup.sh` run |
| `het_installed.md` | Het | Only Het's `setup.sh` run |
| `sakshi_installed.md` | Sakshi | Only Sakshi's `setup.sh` run |

> **Rule:** Only edit YOUR own file. It gets auto-regenerated when you re-run `./setup.sh <your-name>`.
